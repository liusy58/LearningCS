#test error
// call with too many arguments

int fib_rec (n : int) {
  if (n == 0)
    return 1;
  else if (n == 1)
    return 1;
  else
    return fib_rec(n-1) + fib_rec(n-2, 0);
}

int main () {
  var n, result : int;
  n = 10;
  return fib_rec(n);
}
