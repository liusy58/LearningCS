#test return 10
// tests <intconst> and <ident> as an <exp> in a conditional

int main () {
  var i, x : int;
  x = 0;
  for(i = 10; i; i-=1) {
    x += 1;
  }
  if(0)
    return -1;
  else
    return x;
}
