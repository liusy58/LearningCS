#test error
/* variable z is declared but unitialized */
int main()
{
	var i,j,z : int;
	
	for(i = 0; i < 5; i += 1)
	{
		for(j = 0; j < 3; j += 1)
		{
			z += f(i,j)
		}
	}
}

int f(x:int, y:int)
{
	return i << j;
}