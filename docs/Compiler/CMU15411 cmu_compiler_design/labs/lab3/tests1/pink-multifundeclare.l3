#test error
// Two functions with the same name but different types

int fun() {
  return 0;
}

int fun(n : int) {
  return n;
}

int main() {
  if (fun() == fun(0))
    return 0;
  else
    return -1;
}