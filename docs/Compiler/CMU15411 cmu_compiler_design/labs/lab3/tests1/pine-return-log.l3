#test return 7
// Implements a logarithm function, recursively. Not particularly interesting.
int log(a : int) {
    if (a & (~1)) {
        return 1 + log(a >> 1);
    }
    return 0;
}

int main() {
    return log(131);
}
