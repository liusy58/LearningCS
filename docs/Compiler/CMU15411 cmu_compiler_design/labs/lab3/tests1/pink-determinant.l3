#test return 18
// 3 x 3 determinant to test many-parameter-functions 

int det3x3(a : int, b : int, c : int,
           d : int, e : int, f : int,
           g : int, h : int, i : int) {
  return a*e*i + b*f*g + c*d*h - 
        (g*e*c + h*f*a + i*d*b);
}

int main() {
  return det3x3(-2, 2, -3,
                -1, 1, 3,
                2, 0, -1);
}