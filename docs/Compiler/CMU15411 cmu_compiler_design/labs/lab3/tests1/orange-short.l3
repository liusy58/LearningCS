#test return -2
// Tests short circuit evaluation

int main()
{
  var x, y : int;

  x = 0;
  y = -~x;

  if (x && y / x)
    return -1;

  if (y || (x + 1) % !y)
    return -2;
  else
    return 0;
}
