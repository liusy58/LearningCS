#test error
/* declarations must occur at beginning of the function */
int main()
{
	var x,i,y:int;
	x = 0;
	for(i = 0; i < 10; i += 1)
	{
		var z:int;
		z = 0;
		for(x = 1; x < 5; x += 1)
		{
			z -= x * x;
		}
		x += z;
	}
	return z;
}