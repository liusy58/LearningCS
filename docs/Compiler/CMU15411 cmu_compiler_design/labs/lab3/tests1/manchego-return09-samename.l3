#test return 12

int main() {
	var x, y, z, main : int;

	x = 1;
	y = 2;
	z = 3;

	return x(x, y) + y(y, z) + z(x, z);
}

int x(x : int, y : int) {
	return x + y;
}

int y(y : int, z : int) {
	return y + z;
}

int z(x : int, z : int) {
	return x + z;
}
