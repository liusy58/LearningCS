#test return 75

int main () {
  var i, j : int;
  
  var x, y : int;
  x = 0;
  y = 0;
  for(i = 0; i < 10; i += 1)
  {
    for(j = 0; j < 20; j += 1)
    {
      x += 1;
      if (i == j)
        break;
    }
    
    y += 2;
  }
  
  return x + y;
}
