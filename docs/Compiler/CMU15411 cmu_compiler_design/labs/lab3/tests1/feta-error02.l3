#test error

int div (x:int, y:int) {
	var q:int;
	while(1) {
		if (x<y) break;
		q += 1;
		x -= y;
		continue;
		x -= y;
	}
	return q;
}

int div(x:int) {
	return 100/x;
}

int main () {
	return div(111, 222);
}
