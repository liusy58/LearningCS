#test return 0

int returner(i:int)
{
  i = 0;
  if(i < 5)
    return 1;
  else if(i < 10)
    return 2;
  else if(i < 20)
    return 3;
  else if(i < 40)
    return 4;
  else if(i < 80)
    return 5;
  else if(i < 160)
    return 6;
  else if(i < 320)
    return 7;
  else if(i < 640)
    return 8;
  else
    while(1)
      break;
  return 0;
}

int main()
{
  return 0;
}
