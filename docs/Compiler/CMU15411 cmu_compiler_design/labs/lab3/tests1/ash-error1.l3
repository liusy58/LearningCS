#test error

int majn() {
 return 42;
}

int main(argc : int, argv : int) {
  var sum, i : int; 
  sum = 0;
  for (i = 0; i < argc; sum += argv); 
  return sum / argc; 
} 

int mein() {
  return main(0, majn());
}
