#test return 4884
// Lychrel process
{
        p = 87;
        for (i = 0; 1; i += 1) {
       x = p;
       y = 0;
       while (x > 0) {
             y *= 10;
             y += x % 10;
             x /= 10;
       }
        if (y == p)
                return p;
        p += y;
        }
       return -1;
}
