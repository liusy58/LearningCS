#test return 25
// count the primes less than 100
{
        count = 0;
        for (i = 2; i < 100; i += 1) {
                for (j = 2; j < i; j += 1) {
                        if (!(i % j))
                                break;
                }
                if (j == i)
                        count += 1;
        }
        return count;
}
