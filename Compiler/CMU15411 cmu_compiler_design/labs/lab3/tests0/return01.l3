#test return 0
/* Simple function call to sum up numbers */

int main () {
  var n,x,y:int;
  n = 100;
  x = sum(1,100);
  y = n*(n+1)/2;
  return x-y;
}

int sum(i:int, j:int) {
  var sum:int;
  sum = 0;
  for (; i <= j; i+=1)
    sum += i;
  return sum;
}
