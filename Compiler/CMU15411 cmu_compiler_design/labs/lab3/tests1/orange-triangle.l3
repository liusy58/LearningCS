#test return 120
// Tests for loop

int main()
{
  var i, sum : int;

  sum = 0;

  for (i = 1; i < 9; i += 1)
    sum += triangle(i);

  return sum;
}

int triangle(x : int)
{
  var i, sum : int;

  sum = 0;

  for (i = 1; i <= x; i += 1)
    sum += i;

  return sum;
}
