#test return 49
// Finds the least natural less than the given n that is a multiple of 7 iteratively.

int mult7_loop (n : int) {
	var i : int;
  if (n < 0) return -1;
  
  i = 7;
  
  while (i < n) {
    i += 7;
  }
  return i - 7;
}

int main () {
  return mult7_loop(53);
}
