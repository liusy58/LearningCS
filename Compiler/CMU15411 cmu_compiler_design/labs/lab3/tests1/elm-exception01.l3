#test exception 8
// divide by zero in recursion
int main() {
  var y:int;
  y = 1000;
  return anti_factorial(y);
}

int anti_factorial(x:int) {
  if(x == 0)
    return x;
  return x / anti_factorial(x - 1);
}