#test error

int main() {
  var n:int;
  n = 1 * 3 * 5 * 9 * 11;
  return fireinthehole(n,n+1,n+2,n+3,n+4);
}

// One variable is the same as function argument
int fireinthehole(a:int,b:int,c:int,_:int,__:int)
{
  var x,_:int;
  _ = 100;
  x = _ + __;
  if (x == a)
    return 0;
  else if (x == b)
    return 1;
  else if (x != c)
    return 2;
  return _;
}
