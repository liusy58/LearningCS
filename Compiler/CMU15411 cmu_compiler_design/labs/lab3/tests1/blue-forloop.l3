#test return 47
/* Team Blue
 * For loop mechanics
 */

int main() {
	var i: int;
	var accum: int;
	
	accum = 0;
	
	for (i = 0; i < 100; i += 1) {
		if ((i % 3) == 0)
			continue;
		
		if (i > 70)
			break;
			
		accum += 1;
	}
	
	return accum;
}
