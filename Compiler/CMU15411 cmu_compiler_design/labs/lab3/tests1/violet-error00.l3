#test error
/* Tests for return statement in all branches. */

int main() {
  var a, b, c : int;

  a = 7;
  b = 6;
  c = a * b;

  if (a == 1) {
     if (c < 100) {
      return 0;
    } else {
      if (c << 3 >= 100) {
        return 0;
      } else {
        return 12;
      }
    }
  } else if (a == 1) {
    a = 3;
  } else if (a == 2) {
    return 1;
  } else if (a == 3) {
    return 3;
  } else {
    return -100;
  }
}
