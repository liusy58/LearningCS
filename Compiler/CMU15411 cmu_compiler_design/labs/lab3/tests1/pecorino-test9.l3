#test return 1

extern int printhex(x:int);

int do_stuff(x:int){
  var y:int;

  y = printhex(x); 
  return y;
}

int main(){
  var x:int;

  x = 1775544435;
  x = do_stuff(x);

  if (x > 0){
    return 1;
  }
  else{
    if (x < 0){
      return 1;
    }
  }
  return 0; //x = 0
}
