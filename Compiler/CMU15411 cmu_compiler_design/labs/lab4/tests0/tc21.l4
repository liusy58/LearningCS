#test error
/* function multiply declared */

int fib (x:int) {
  if (x == 0) return 1;
  if (x == 1) return 1;
  return fib(x, x-1);
}

int fib (x1:int, x2:int) {
  var y:int;
  return x1+x2;
}

int main () {
  return 0;
}
