#test return 1
// test overloading of == and !=

int main ()
{
        var a, b, c : int;
        var d, e, f : int*;

        d = new (int);
        e = new (int);
        f = e;
        a = 10;
        b = 10;
        c = 9;

        return (f == e) && (d != e) && (a == b) && (a != c) &&
                (b != c) && (f != d) ? 1 : 0;
}
